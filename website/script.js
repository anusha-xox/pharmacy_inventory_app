const toggle=document.querySelector('.menu-toggle');const menu=document.querySelector('.nav-menu');if(toggle&&menu){toggle.addEventListener('click',()=>{const open=menu.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open));});}
const form=document.querySelector('#appointment');if(form){form.addEventListener('submit',(e)=>{e.preventDefault();const data=new FormData(form);const name=data.get('name')||'Patient';const phone=data.get('phone')||'';const concern=data.get('concern')||'ENT Consultation';const date=data.get('date')||'preferred date not selected';const msg=`Appointment request prepared for ${name}. Please call 086709 84774 to confirm the booking. Concern: ${concern}. Preferred date: ${date}. Phone: ${phone}.`;const toast=document.createElement('div');toast.className='toast';toast.textContent=msg;document.body.appendChild(toast);setTimeout(()=>toast.remove(),6500);});}
